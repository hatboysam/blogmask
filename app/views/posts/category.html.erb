<h1><%= @category %></h1>
<hr>
<% if (@posts.count == 0) %>
  Sorry, there are no posts for this category yet.  
  Why not <%= link_to "write one", new_post_path %>?
<% else %>
  <% @posts.each_slice(3) do |slice| %>
    <div class="row">
      <% slice.each do |p| %>
        <div class="small-4 large-4 columns panel post-panel">
          <span class="no-over"><%= link_to "#{p.title} (+#{p.numvotes})", post_slug_path(p.hashed_key, p.title_slug) %></span>
        </div>
      <% end %>
    </div>
  <% end %>
<% end %>